import Country from "@site/src/components/Country";
import JoinLineBreak from "@site/src/components/JoinLineBreak";
import SortableTable from "@site/src/components/SortableTable";
import alliances from "@site/src/data/airline-alliances.ts";

# 🚩 Airline Alliance

Airline alliance at a glance.

<SortableTable
  columns={[
    {
      header: "Countries",
      accessorKey: "countries",
      cell: ({ getValue }) => {
        const countries = getValue();
        return JoinLineBreak(
          countries
            .split(",")
            .map((country) => <Country key={country} iso={country} />),
        );
      },
    },
    {
      header: "One World",
      accessorKey: "One World",
      cell: ({ getValue }) => {
        const airlines = getValue();
        return airlines
          ? JoinLineBreak(airlines.map((airline) => airline.name))
          : null;
      },
    },
    {
      header: "SkyTeam",
      accessorKey: "SkyTeam",
      cell: ({ getValue }) => {
        const airlines = getValue();
        return airlines
          ? JoinLineBreak(airlines.map((airline) => airline.name))
          : null;
      },
    },
    {
      header: "Star Alliance",
      accessorKey: "Star Alliance",
      cell: ({ getValue }) => {
        const airlines = getValue();
        return airlines
          ? JoinLineBreak(airlines.map((airline) => airline.name))
          : null;
      },
    },
    {
      header: "Value Alliance",
      accessorKey: "Value Alliance",
      cell: ({ getValue }) => {
        const airlines = getValue();
        return airlines
          ? JoinLineBreak(airlines.map((airline) => airline.name))
          : null;
      },
    },
  ]}
  data={alliances}
  defaultSorting={{ id: "countries", desc: false }}
/>
