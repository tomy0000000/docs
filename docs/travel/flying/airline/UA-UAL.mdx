import airlines from "@site/src/data/airlines.json";

# United Airlines

<ul>
  {airlines["UA-UAL"].offers.map((offerLink, index) => (
    <li>
      <a
        href={offerLink.url}
        target="_blank"
        rel="noopener noreferrer"
        key={index}
      >
        {offerLink.name}
      </a>
    </li>
  ))}
</ul>
