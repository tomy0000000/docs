---
sidebar_position: 2
skip_plural_check: true
---

import { Icon } from "@iconify/react";
import Country from "@site/src/components/Country";
import SortableTable from "@site/src/components/SortableTable";
import transitPasses from "@site/src/data/transit-passes.json";

# 💳 Pass

A list of transit passes around the world.

<SortableTable
  columns={[
    {
      header: "Pass",
      accessorKey: "name",
      accessorFn: (row) => [row.name, row["name-en"]],
      cell: ({ getValue }) => {
        const [name, nameEn] = getValue();
        return name === nameEn ? name : `${nameEn} (${name})`;
      },
    },
    {
      header: "Apple Pay",
      accessorKey: "apple-pay",
      cell: ({ getValue }) =>
        getValue() ? <Icon icon="logos:apple-pay" inline={true} /> : "❌",
    },
    {
      header: "Country",
      accessorKey: "country",
      cell: ({ getValue }) => <Country iso={getValue()} />,
    },
  ]}
  data={Object.values(transitPasses)}
/>
