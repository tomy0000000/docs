# 🔗 Referral

import { Icon } from "@iconify/react";
import referrals from "@site/src/data/referral.json";
import CodeBlock from "@theme/CodeBlock";

export function ReferralItem(props) {
  const { id, item } = props;
  return (
    <>
      <h2 id={id} className={id}>
        {item.icon && (
          <>
            <Icon icon={item.icon} inline={true} />{" "}
          </>
        )}
        {item.name}
      </h2>
      <p>
        {item.dynamic ? (
          "Please reach out to me for the latest referral link or code."
        ) : (
          <>
            <ul>
              <li>
                What you get: {item["for-you"] ? item["for-you"] : "Nothing 🥲"}
              </li>
              <li>What I get: {item["for-me"]}</li>
              <li>
                How to redeem:{" "}
                {item.url && (
                  <a href={item.url} target="_blank" rel="noopener noreferrer">
                    Link
                  </a>
                )}
                {item.url && item.code && " or "}
                {item.code && "code"}
              </li>
            </ul>
            {item.code && <CodeBlock>{item.code}</CodeBlock>}
          </>
        )}
      </p>
    </>
  );
}

export function ReferralItems(props) {
  return (
    <>
      {Object.keys(props.referrals).map((key) => (
        <ReferralItem id={key} item={props.referrals[key]} />
      ))}
    </>
  );
}

export const toc = Object.keys(referrals).map((key) => ({
  value: referrals[key].name,
  id: key,
  level: 2,
}));

A list of referral link of code to support me 🫶🏻

<ReferralItems referrals={referrals} />
