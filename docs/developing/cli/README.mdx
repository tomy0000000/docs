---
hide_table_of_contents: true
full_width: true
---

import JoinLineBreak from "@site/src/components/JoinLineBreak";
import SortableTable from "@site/src/components/SortableTable";
import clis from "@site/src/data/clis.json";

# 📟 CLIs

A list of common CLI commands and how they're installed on my system.

<SortableTable
  columns={[
    {
      header: "Name",
      accessorFn: (row) => [row.name, row.url],
      cell: ({ getValue }) => {
        const [name, url] = getValue();
        return url ? (
          <a href={url} target="blank" rel="noreferrer noopener">
            {name}
          </a>
        ) : (
          name
        );
      },
    },
    {
      header: "CLIs",
      accessorKey: "clis",
      size: 300,
      cell: ({ getValue }) => {
        const clis = getValue();
        return JoinLineBreak(
          clis.map((name, index) => <code key={index}>{name}</code>),
        );
      },
    },
    {
      header: "Category",
      accessorKey: "category",
    },
    {
      header: "macOS Zsh Completion",
      accessorKey: "macos-zsh-completion",
      cell: ({ getValue }) => {
        const completion = getValue();
        return completion ? completion : null;
      },
    },
    {
      header: "Linux Zsh Completion",
      accessorKey: "linux-zsh-completion",
      cell: ({ getValue }) => {
        const completion = getValue();
        return completion ? completion : null;
      },
    },
    {
      header: "macOS Alias",
      accessorKey: "macos-alias",
      cell: ({ getValue }) => {
        const alias = getValue();
        return alias ? <code>{alias}</code> : null;
      },
    },
    {
      header: "Linux Alias",
      accessorKey: "linux-alias",
      cell: ({ getValue }) => {
        const alias = getValue();
        return alias ? <code>{alias}</code> : null;
      },
    },
  ]}
  data={clis}
/>

## macOS Zsh Completion

- Built-in completions:
  - `/usr/share/zsh/5.9/functions`
  - `/usr/share/zsh/site-functions`
- Homebrew completions:
  - `/usr/local/share/zsh/site-functions`
  - `/usr/local/share/zsh-completions` (When `zsh-completions` formula is installed)

## References

- [Modern Unix](https://github.com/ibraheemdev/modern-unix)
