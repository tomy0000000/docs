import { Icon } from "@iconify/react";
import Country from "@site/src/components/Country";
import SortableTable from "@site/src/components/SortableTable";
import maps from "@site/src/data/maps.json";

# 🗺️ Maps

List of online maps that can be used in the frontend.

<SortableTable
  columns={[
    {
      header: "Name",
      size: 200,
      accessorFn: (row) => [row.name, row["web-app"], row.country, row.icon],
      cell: ({ getValue }) => {
        const [name, webApp, country, icon] = getValue();
        return (
          <>
            {icon && (
              <>
                <Icon icon={icon} inline={true} />{" "}
              </>
            )}
            {country && (
              <>
                <Country key={country} iso={country} name={false} />{" "}
              </>
            )}
            <a href={webApp} target="_blank" rel="noopener noreferrer">
              {name}
            </a>
          </>
        );
      },
    },
    {
      header: "Web",
      accessorKey: "web-docs",
      cell: ({ getValue }) => {
        const url = getValue();
        return url ? (
          <a href={url} target="_blank" rel="noopener noreferrer">
            Docs
          </a>
        ) : (
          "-"
        );
      },
    },
    {
      header: "iOS",
      accessorKey: "ios-docs",
      cell: ({ getValue }) => {
        const url = getValue();
        return url ? (
          <a href={url} target="_blank" rel="noopener noreferrer">
            Docs
          </a>
        ) : (
          "-"
        );
      },
    },
    {
      header: "Android",
      accessorKey: "android-docs",
      cell: ({ getValue }) => {
        const url = getValue();
        return url ? (
          <a href={url} target="_blank" rel="noopener noreferrer">
            Docs
          </a>
        ) : (
          "-"
        );
      },
    },
  ]}
  data={Object.values(maps)}
/>
