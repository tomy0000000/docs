---
---

# Healthcheck

```mdx-code-block
import { Icon } from '@iconify/react';
import PacageManagerCommands from '@site/src/components/PacageManagerCommands';
import commands from '@site/src/data/package-manager-commands.json';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
```

Run a healthcheck / diagnosis to verify the package manager / installation is working correctly.

<PacageManagerCommands commands={commands.healthcheck} />
